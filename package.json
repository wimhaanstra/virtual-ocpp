{
  "name": "virtual-ocpp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspaces --if-present",
    "dev:server": "npm run dev --workspace=@virtual-ocpp/server",
    "dev:web": "npm run dev --workspace=@virtual-ocpp/web",
    "dev:stop": "node scripts/stop-dev-processes.mjs",
    "simulator": "npm run simulator --workspace=@virtual-ocpp/server --",
    "smoke:simulator": "npm run simulator -- --smoke",
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "db:migrate": "npm run db:migrate --workspace=@virtual-ocpp/server",
    "docker:build": "docker build -t sortedbit/virtual-ocpp:latest -t sortedbit/virtual-ocpp:$npm_package_version .",
    "docker:build:amd64": "docker buildx build --platform linux/amd64 -t sortedbit/virtual-ocpp:$npm_package_version-amd64 --load .",
    "docker:push": "npm run docker:publish",
    "docker:publish": "docker buildx build --platform linux/amd64,linux/arm64 -t sortedbit/virtual-ocpp:latest -t sortedbit/virtual-ocpp:$npm_package_version --push ."
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
